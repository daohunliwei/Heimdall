# Heimdall 联调问题记录

## 问题 1-8：已修复（路由、DI、Dashboard、Owner 解析、FK 违规、XML 解析、Token Stat、截断）
详见上一版本。

---

## 问题 9：LLM 调用链路三重 BUG（已修复）
- `/api/generate` → `/api/chat`，模型参数不再丢弃，think 标签正则清理
- `num_ctx: 65536`, `temperature: 0.3`

---

## 问题 10（致命）：RAG 流水线缺失 — LLM 从未拿到文件内容

**严重程度**: 致命（Wiki 内容是幻觉）

### 现象
生成的 Wiki 页面内容与真实代码完全不符。LLM 只看到文件路径列表，从未看到文件内容。

### 根因对比

| 步骤 | deepwiki-open | Heimdall（修复前） |
|------|--------------|-------------------|
| **克隆后** | `prepare_database()` → 读文件 → 分块 → 嵌入 → 存 FAISS 索引 | 仅克隆，未嵌入 |
| **页面生成前** | `RAG.call(query)` → FAISS 相似搜索 → 返回相关代码块 | **未调用 RAG** |
| **提示词构建** | `<START_OF_CONTEXT>` + 实际文件内容 + `<END_OF_CONTEXT>` | 仅文件路径链接（URL），LLM 无法访问 |
| **提示词声称** | 提供真实代码 | "You have access to the full content of these files through the RAG system" — **这是谎言** |

### 修复（本轮）

1. **`ReadPageFiles()` 新增** — 从已克隆仓库直接读取 `page.FilePaths` 中列出的文件内容（每文件上限 12KB）
2. **`BuildWikiPagePrompt()` 重构** — 接受 `fileContents` 参数，将真实文件内容嵌入提示词 `[RELEVANT_SOURCE_FILES]` 区域
3. **嵌入流水线后台启动** — 克隆完成后异步启动 `EmbedRepoAsync()`，为后续 RAG 聊天做准备
4. **提示词不再说谎** — "Based ONLY on the actual source file contents provided above"

### 待实现（Agent Loop）

当前：单次 LLM 调用生成一个页面（含文件内容）。

---

## 问题 11：嵌入 chunk 溢出 + 并发冲突（本轮验证发现）

### 嵌入失败
- **现象**: `Ollama embedding API error (500): "the input length exceeds the context length"` (InputLength=5456)
- **原因**: `text_splitter.chunk_size=350` 词，对代码文件 token 化后可能超过 nomic-embed-text 上下文限制
- **修复**: `chunk_size: 350→200`, `chunk_overlap: 100→50`

### 并发冲突
- **现象**: `DbUpdateConcurrencyException: The database operation was expected to affect 1 row(s), but actually affected 0 row(s)`
- **原因**: `LogLlmCallAsync` 和 `UpdateTaskAsync` 使用不同 scope 同时更新同一 task 实体
- **修复**: `TaskRepository.UpdateStatusAsync` 添加 3 次重试 + `ReloadAsync()` 逻辑
