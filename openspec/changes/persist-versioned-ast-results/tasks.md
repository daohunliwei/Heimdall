## 1. AST 数据模型

- [ ] 1.1 设计并新增 AST 版本主记录实体，绑定 `RepositoryVersion`，补齐分支、提交、解析配置、状态、统计与时间字段
- [ ] 1.2 设计并新增 AST 文件、符号、调用边、依赖边、分块和模式提示等明细实体或等价结构化持久化模型
- [ ] 1.3 为 AST 版本和明细模型补齐索引、唯一键与注释，明确同快照复用与多版本共存边界
- [ ] 1.4 扩展 `WikiVersion`，增加对实际依赖 `AstVersionId` 的追溯字段

## 2. AST 持久化链路

- [ ] 2.1 在代码分析阶段产出可直接落库的 AST 结构化投影，而不是仅保留运行时对象或字符串摘要
- [ ] 2.2 新增 AST 版本仓储和批量写入路径，支持主记录与关键明细的事务式提交
- [ ] 2.3 为 AST 持久化补齐成功、失败、复用和恢复锚点状态，使失败版本不会被下游引用

## 3. 版本解析与复用

- [ ] 3.1 建立按 `RepositoryVersion`、分支、提交和解析配置查询 AST 版本的读取路径
- [ ] 3.2 实现同一快照命中相同解析配置时的 AST 版本复用逻辑
- [ ] 3.3 验证同仓库不同分支、同分支不同提交、同快照不同解析配置的 AST 版本可长期共存

## 4. Wiki 版本绑定

- [ ] 4.1 调整 Wiki 主链路，确保在持久化 `WikiVersion` 前先解析或复用可引用的 AST 版本
- [ ] 4.2 在 `WikiVersion` 落库、任务结果摘要和版本化知识读取路径中写入并暴露 `AstVersionId`
- [ ] 4.3 阻断“AST 持久化失败但 Wiki 成功落库”的状态撕裂场景

## 5. 验证与回归

- [ ] 5.1 为 AST 版本主记录、明细落库、版本复用和失败隔离补齐后端测试
- [ ] 5.2 为 Wiki 与 AST 的绑定关系补齐验证，确保可精确追溯 `repository_version_id + ast_version_id + wiki_version_id`
- [ ] 5.3 运行后端构建与相关测试，确认新增 AST 版本化持久化链路不破坏现有 Wiki 生成主流程
